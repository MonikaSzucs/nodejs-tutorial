const num1 = 5;
const num2 = 10;

function addValues(){
    console.log(`the sum is: ${num1 + num2}`)
}

// if you have some kind of function it will invoke the code automatically
// this means you do not need to use exports
addValues()